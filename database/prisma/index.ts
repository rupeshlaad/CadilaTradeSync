// Re-export the generated Prisma client so consumers can import from '@cts/database'.
// The client itself is generated into ./node_modules/@prisma/client by prisma:generate.
export * from '@prisma/client';
export { PrismaClient as default } from '@prisma/client';
