# Master Watcher — polling cadence for master accounts (milliseconds).
# Increase to reduce broker API calls (e.g. ICICI) and log noise.
# Defaults to 30000 (30s) when unset.
MASTER_WATCHER_POLL_INTERVAL_MS=30000
